[package]
name = "wasm-erasure"
version = "0.2.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2.84"
js-sys = "0.3.61"
reed-solomon-erasure = "6.0"
instant = { version = "0.1", features = ["wasm-bindgen"] }
